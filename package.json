{
  "name": "coral-reward-watchtower",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:coral": "node scripts/generate-coral-spec.mjs",
    "rank": "node scripts/rank-candidates.mjs",
    "demo": "npm run prepare:coral && coral source lint .coral-local/reward-signals.yaml && coral source add --file .coral-local/reward-signals.yaml && coral source test reward_signals && coral sql \"SELECT id, title, reward_type, reward_value, verdict, fit_score, next_action FROM reward_signals.candidates ORDER BY fit_score DESC LIMIT 6\""
  }
}
